\newcommand{\ConclusionContent}{%

ابزار \code{awtr} خروجی \code{TTS} افرا را می‌خواند، تعامل‌های خارج از مجموعهٔ
مشاهده را پنهان می‌کند و مدل خارج‌قسمتی را همراه با نگاشت هر حالت ورودی به ردهٔ
خود تحویل می‌دهد.

\vspace{.10cm}
{\postertitlefont\fontsize{28}{36}\selectfont کاربرد\par}

{\fontsize{21}{28}\selectfont
\PosterLead{بررسی چشمی یک \code{TTS}:} نمودار اصلی به‌سرعت ناخوانا می‌شود. نسخهٔ
کاهش‌یافته همان رفتار قابل مشاهده را در نموداری نگه می‌دارد که می‌توان با چشم
دنبال کرد.\par
\PosterLead{مقایسهٔ پیاده‌سازی و مشخصه:} اگر این کاهش به‌صورت \code{on the fly}
و هم‌زمان با تولید فضای حالت اعمال شود، می‌توان دو مدل ربکا را — یکی به‌عنوان
\code{Implementation} و دیگری به‌عنوان \code{Specification} — نسبت به مجموعهٔ
تعامل‌های مورد نظر با هم سنجید.\par}

\PosterHighlight[PosterBlue]{%
کاهش در وضعیت فعلی \emph{پس از} ساخته‌شدن \code{TTS} انجام می‌شود. بنابراین
مسئلهٔ انفجار فضای حالت در مرحلهٔ تولید مدل همچنان باقی است و آنچه این ابزار
ارائه می‌دهد، کوچک‌کردن مدلِ ساخته‌شده است.}

{\postertitlefont\fontsize{28}{36}\selectfont گام‌های بعدی\par}

{\fontsize{21}{28}\selectfont
\PosterLead{افزونهٔ افرا:} افزودن آداپتور درون‌فرایندی و فرمان متناظر در خود
افرا، تا مدل بدون فایل واسط به هستهٔ کاهش برسد و نتیجه در همان محیط دیده شود.\par
\PosterLead{کاهش هم‌زمان با تولید:} اعمال کاهش در حین ساخت فضای حالت، که راه
مقایسهٔ مستقیم دو مدل را باز می‌کند.\par
}
}
